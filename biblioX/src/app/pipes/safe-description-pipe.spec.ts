import { SafeDescriptionPipe } from './safe-description-pipe';

describe('SafeDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
